package com.mijao.poc.persistence.civil;

import org.junit.Ignore;
import org.junit.Test;

public class IncopServiceTest extends TestBase {

    @Test
    @Ignore("Integration test.")
    public void testGetByCedula() {

        //IncopService incopService = (IncopService) getBean(IncopService.BEAN_NAME);
        //IncopEntidad entidad = incopService.getEntidadOrProveedor("1718275801");

        // assertNotNull(entidad);

        // assertTrue(entidad instanceof Proveedor);
    }
}
